-- Universal Flexible Approval System - Complete Setup
-- Run each script individually in your database

-- STEP 1: Create the schema (8 tables)
-- Run: database/flexible_approval_schema.sql

-- STEP 2: Implement smart routing system (80/15/5 logic)
-- Run: database/practical_approval_system.sql

-- STEP 3: Add approval tiles to UI (28 tiles)
-- Run: database/add_flexible_approval_tiles.sql

-- STEP 4: Run end-to-end tests (5 scenarios)
-- Run: database/test_end_to_end_workflow.sql

-- Quick test: Run practical_approval_system.sql to see smart routing in action
SELECT 'COPY AND RUN EACH SCRIPT INDIVIDUALLY' as instruction;